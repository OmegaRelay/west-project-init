image: golang:latest

build:
  stage: build
  variables:
    GOOS: darwin
    GOARCH: arm64
  script:
    - go build -o out/
  artifacts:
    paths:
      - out
  rules:
    # Only push when committed to the main branch
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

stages:
  - build
